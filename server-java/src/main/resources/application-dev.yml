# 开发环境配置
spring:
  mail:
    # QQ邮箱SMTP服务器配置
    host: smtp.qq.com
    port: 465
    # 请替换为你的QQ邮箱
    username: 1843599766@qq.com
    # 请替换为你的QQ邮箱授权码（不是QQ密码）
    # 获取方式：QQ邮箱 -> 设置 -> 账户 -> POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务 -> 生成授权码
    password: lgyeeiikszmnccie
    protocol: smtps
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

app:
  auth:
    verification:
      # 发件人邮箱（与上面的username保持一致）
      mail-from: 1843599766@qq.com
