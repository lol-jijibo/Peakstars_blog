<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.interview.auth.admin.mapper.AdminMapper">

  <resultMap id="TechArticleAdminResultMap" type="com.interview.auth.domain.entity.TechArticle">
    <id column="id" property="id"/>
    <result column="article_key" property="articleKey"/>
    <result column="category" property="category"/>
    <result column="category_label" property="categoryLabel"/>
    <result column="title" property="title"/>
    <result column="summary" property="summary"/>
    <result column="essence" property="essence"/>
    <result column="highlight_list" property="highlightList"/>
    <result column="author_name" property="authorName"/>
    <result column="author_role" property="authorRole"/>
    <result column="author_initials" property="authorInitials"/>
    <result column="author_accent" property="authorAccent"/>
    <result column="cover_url" property="coverUrl"/>
    <result column="content_html" property="contentHtml"/>
    <result column="published_at" property="publishedAt"/>
    <result column="read_count" property="readCount"/>
    <result column="like_count" property="likeCount"/>
    <result column="collect_count" property="collectCount"/>
    <result column="comment_count" property="commentCount"/>
    <result column="read_time" property="readTime"/>
    <result column="is_vip" property="isVip"/>
    <result column="is_collected" property="isCollected"/>
    <result column="is_liked" property="isLiked"/>
    <result column="in_history" property="inHistory"/>
    <result column="featured" property="featured"/>
    <result column="status" property="status"/>
    <result column="sort_order" property="sortOrder"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>

  <resultMap id="WorldNewsAdminResultMap" type="com.interview.auth.domain.entity.WorldNewsIssue">
    <id column="id" property="id"/>
    <result column="issue_key" property="issueKey"/>
    <result column="title" property="title"/>
    <result column="issue_label" property="issueLabel"/>
    <result column="category" property="category"/>
    <result column="today_reads" property="todayReads"/>
    <result column="recommendation" property="recommendation"/>
    <result column="description" property="description"/>
    <result column="cover_accent" property="coverAccent"/>
    <result column="cover_kicker" property="coverKicker"/>
    <result column="cover_headline" property="coverHeadline"/>
    <result column="cover_summary" property="coverSummary"/>
    <result column="cover_footer" property="coverFooter"/>
    <result column="content_html" property="contentHtml"/>
    <result column="published_at" property="publishedAt"/>
    <result column="status" property="status"/>
    <result column="sort_order" property="sortOrder"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>

  <resultMap id="ContentEditLogResultMap" type="com.interview.auth.admin.entity.ContentEditLog">
    <id column="id" property="id"/>
    <result column="content_type" property="contentType"/>
    <result column="content_key" property="contentKey"/>
    <result column="action_type" property="actionType"/>
    <result column="operator_name" property="operatorName"/>
    <result column="content_title" property="contentTitle"/>
    <result column="created_at" property="createdAt"/>
  </resultMap>

  <resultMap id="InterviewAdminResultMap" type="com.interview.auth.domain.entity.Interview">
    <id column="id" property="id"/>
    <result column="title" property="title"/>
    <result column="author" property="author"/>
    <result column="summary" property="summary"/>
    <result column="content" property="content"/>
    <result column="cover_url" property="coverUrl"/>
    <result column="views" property="views"/>
    <result column="likes" property="likes"/>
    <result column="collects" property="collects"/>
    <result column="publish_date" property="publishDate"/>
    <result column="difficulty" property="difficulty"/>
    <result column="tag_list" property="tagList"/>
    <result column="company_id" property="companyId"/>
    <result column="category_id" property="categoryId"/>
    <result column="status" property="status"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>

  <select id="countPublishedInterviews" resultType="java.lang.Integer">
    SELECT COUNT(*) FROM interview WHERE status = 1
  </select>

  <select id="sumInterviewViews" resultType="java.lang.Integer">
    SELECT COALESCE(SUM(views), 0) FROM interview WHERE status = 1
  </select>

  <select id="countPublishedTechArticles" resultType="java.lang.Integer">
    SELECT COUNT(*) FROM tech_article WHERE status = 1
  </select>

  <select id="countPublishedWorldNewsIssues" resultType="java.lang.Integer">
    SELECT COUNT(*) FROM world_news_issue WHERE status = 1
  </select>

  <select id="sumTechArticleViews" resultType="java.lang.Integer">
    SELECT COALESCE(SUM(read_count), 0) FROM tech_article WHERE status = 1
  </select>

  <select id="sumWorldNewsReads" resultType="java.lang.Integer">
    SELECT COALESCE(SUM(today_reads), 0) FROM world_news_issue WHERE status = 1
  </select>

  <select id="sumTechArticleComments" resultType="java.lang.Integer">
    SELECT COALESCE(SUM(comment_count), 0) FROM tech_article WHERE status = 1
  </select>

  <select id="countTodayEdits" resultType="java.lang.Integer">
    SELECT COUNT(*) FROM content_edit_log WHERE DATE(created_at) = CURRENT_DATE
  </select>

  <select id="findRecentEditLogs" resultMap="ContentEditLogResultMap">
    SELECT id, content_type, content_key, action_type, operator_name, content_title, created_at
    FROM content_edit_log
    ORDER BY created_at DESC
    LIMIT #{limit}
  </select>

  <select id="findAllTechArticles" resultMap="TechArticleAdminResultMap">
    SELECT
      id, article_key, category, category_label, title, summary, essence,
      highlight_list, author_name, author_role, author_initials, author_accent,
      cover_url, published_at, read_count, like_count, collect_count, comment_count,
      read_time, is_vip, is_collected, is_liked, in_history, featured, status, sort_order,
      created_at, updated_at
    FROM tech_article
    WHERE status = 1
    ORDER BY published_at DESC, featured DESC, sort_order ASC
  </select>

  <select id="findAllWorldNewsIssues" resultMap="WorldNewsAdminResultMap">
    SELECT
      id, issue_key, title, issue_label, category, today_reads, recommendation,
      description, cover_accent, cover_kicker, cover_headline, cover_summary, cover_footer,
      published_at, status, sort_order, created_at, updated_at
    FROM world_news_issue
    WHERE status = 1
    ORDER BY published_at DESC, sort_order ASC
  </select>

  <insert id="saveTechArticle">
    INSERT INTO tech_article (
      article_key,
      category,
      category_label,
      title,
      summary,
      essence,
      highlight_list,
      author_name,
      author_role,
      author_initials,
      author_accent,
      cover_url,
      content_html,
      published_at,
      read_count,
      like_count,
      collect_count,
      comment_count,
      read_time,
      is_vip,
      is_collected,
      is_liked,
      in_history,
      featured,
      status,
      sort_order
    ) VALUES (
      #{articleKey},
      #{category},
      #{categoryLabel},
      #{title},
      #{summary},
      #{essence},
      #{highlightList},
      #{authorName},
      #{authorRole},
      #{authorInitials},
      #{authorAccent},
      #{coverUrl},
      #{contentHtml},
      #{publishedAt},
      #{readCount},
      #{likeCount},
      #{collectCount},
      #{commentCount},
      #{readTime},
      #{isVip},
      #{isCollected},
      #{isLiked},
      #{inHistory},
      #{featured},
      #{status},
      #{sortOrder}
    )
    ON DUPLICATE KEY UPDATE
      category = VALUES(category),
      category_label = VALUES(category_label),
      title = VALUES(title),
      summary = VALUES(summary),
      essence = VALUES(essence),
      highlight_list = VALUES(highlight_list),
      author_name = VALUES(author_name),
      author_role = VALUES(author_role),
      author_initials = VALUES(author_initials),
      author_accent = VALUES(author_accent),
      cover_url = VALUES(cover_url),
      content_html = VALUES(content_html),
      published_at = VALUES(published_at),
      read_count = VALUES(read_count),
      like_count = VALUES(like_count),
      collect_count = VALUES(collect_count),
      comment_count = VALUES(comment_count),
      read_time = VALUES(read_time),
      is_vip = VALUES(is_vip),
      is_collected = VALUES(is_collected),
      is_liked = VALUES(is_liked),
      in_history = VALUES(in_history),
      featured = VALUES(featured),
      status = VALUES(status),
      sort_order = VALUES(sort_order),
      updated_at = CURRENT_TIMESTAMP
  </insert>

  <insert id="saveWorldNewsIssue">
    INSERT INTO world_news_issue (
      issue_key,
      title,
      issue_label,
      category,
      today_reads,
      recommendation,
      description,
      cover_accent,
      cover_kicker,
      cover_headline,
      cover_summary,
      cover_footer,
      content_html,
      published_at,
      status,
      sort_order
    ) VALUES (
      #{issueKey},
      #{title},
      #{issueLabel},
      #{category},
      #{todayReads},
      #{recommendation},
      #{description},
      #{coverAccent},
      #{coverKicker},
      #{coverHeadline},
      #{coverSummary},
      #{coverFooter},
      #{contentHtml},
      #{publishedAt},
      #{status},
      #{sortOrder}
    )
    ON DUPLICATE KEY UPDATE
      title = VALUES(title),
      issue_label = VALUES(issue_label),
      category = VALUES(category),
      today_reads = VALUES(today_reads),
      recommendation = VALUES(recommendation),
      description = VALUES(description),
      cover_accent = VALUES(cover_accent),
      cover_kicker = VALUES(cover_kicker),
      cover_headline = VALUES(cover_headline),
      cover_summary = VALUES(cover_summary),
      cover_footer = VALUES(cover_footer),
      content_html = VALUES(content_html),
      published_at = VALUES(published_at),
      status = VALUES(status),
      sort_order = VALUES(sort_order),
      updated_at = CURRENT_TIMESTAMP
  </insert>

  <update id="disableTechArticle">
    UPDATE tech_article
    SET status = 0, updated_at = CURRENT_TIMESTAMP
    WHERE article_key = #{articleKey} AND status = 1
  </update>

  <update id="disableWorldNewsIssue">
    UPDATE world_news_issue
    SET status = 0, updated_at = CURRENT_TIMESTAMP
    WHERE issue_key = #{issueKey} AND status = 1
  </update>

  <select id="findAllInterviews" resultMap="InterviewAdminResultMap">
    SELECT
      id, company_id, category_id, title, author, summary, content, cover_url,
      publish_date, views, likes, collects, difficulty, tag_list,
      status, created_at, updated_at
    FROM interview
    WHERE status = 1
    ORDER BY publish_date DESC, created_at DESC
  </select>

  <select id="findCategoryById" resultType="com.interview.auth.domain.entity.Category">
    SELECT id, code, name, sort_order
    FROM category
    WHERE id = #{categoryId}
    LIMIT 1
  </select>

  <select id="findCategoryByCode" resultType="com.interview.auth.domain.entity.Category">
    SELECT id, code, name, sort_order
    FROM category
    WHERE code = #{categoryCode}
    LIMIT 1
  </select>

  <insert id="saveInterview" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO interview (
      id, company_id, category_id, title, author, summary, content, cover_url,
      views, likes, collects, publish_date, difficulty, tag_list, status
    ) VALUES (
      #{id}, #{companyId}, #{categoryId}, #{title}, #{author}, #{summary}, #{content}, #{coverUrl},
      #{views}, #{likes}, #{collects}, #{publishDate}, #{difficulty}, #{tagList}, #{status}
    )
    ON DUPLICATE KEY UPDATE
      company_id = VALUES(company_id),
      category_id = VALUES(category_id),
      title = VALUES(title),
      author = VALUES(author),
      summary = VALUES(summary),
      content = VALUES(content),
      cover_url = VALUES(cover_url),
      views = VALUES(views),
      likes = VALUES(likes),
      collects = VALUES(collects),
      publish_date = VALUES(publish_date),
      difficulty = VALUES(difficulty),
      tag_list = VALUES(tag_list),
      status = VALUES(status),
      updated_at = CURRENT_TIMESTAMP
  </insert>

  <update id="disableInterview">
    UPDATE interview
    SET status = 0, updated_at = CURRENT_TIMESTAMP
    WHERE id = #{interviewId} AND status = 1
  </update>

  <insert id="insertContentEditLog" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO content_edit_log (
      content_type,
      content_key,
      action_type,
      operator_name,
      content_title
    ) VALUES (
      #{contentType},
      #{contentKey},
      #{actionType},
      #{operatorName},
      #{contentTitle}
    )
  </insert>

  <!-- ── 草稿管理 ──────────────────────────────────────── -->

  <resultMap id="ContentDraftResultMap" type="com.interview.auth.admin.entity.ContentDraft">
    <id column="id" property="id"/>
    <result column="draft_key" property="draftKey"/>
    <result column="content_type" property="contentType"/>
    <result column="title" property="title"/>
    <result column="content_html" property="contentHtml"/>
    <result column="extra_json" property="extraJson"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>

  <select id="findDraftsByType" resultMap="ContentDraftResultMap">
    SELECT id, draft_key, content_type, title, content_html, extra_json, created_at, updated_at
    FROM content_draft
    WHERE content_type = #{contentType}
    ORDER BY updated_at DESC
  </select>

  <select id="findDraftByKey" resultMap="ContentDraftResultMap">
    SELECT id, draft_key, content_type, title, content_html, extra_json, created_at, updated_at
    FROM content_draft
    WHERE draft_key = #{draftKey}
  </select>

  <insert id="upsertDraft">
    INSERT INTO content_draft (draft_key, content_type, title, content_html, extra_json)
    VALUES (#{draftKey}, #{contentType}, #{title}, #{contentHtml}, #{extraJson})
    ON DUPLICATE KEY UPDATE
      title       = VALUES(title),
      content_html = VALUES(content_html),
      extra_json  = VALUES(extra_json),
      updated_at  = CURRENT_TIMESTAMP
  </insert>

  <delete id="deleteDraft">
    DELETE FROM content_draft WHERE draft_key = #{draftKey}
  </delete>
</mapper>
