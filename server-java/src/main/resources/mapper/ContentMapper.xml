<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.interview.auth.infrastructure.mapper.ContentMapper">

  <resultMap id="TechArticleResultMap" type="com.interview.auth.domain.entity.TechArticle">
    <id column="id" property="id"/>
    <result column="article_key" property="articleKey"/>
    <result column="category" property="category"/>
    <result column="title" property="title"/>
    <result column="summary" property="summary"/>
    <result column="essence" property="essence"/>
    <result column="highlight_list" property="highlightList"/>
    <result column="author_name" property="authorName"/>
    <result column="author_role" property="authorRole"/>
    <result column="author_initials" property="authorInitials"/>
    <result column="author_accent" property="authorAccent"/>
    <result column="cover_url" property="coverUrl"/>
    <result column="content_html" property="contentHtml"/>
    <result column="published_at" property="publishedAt"/>
    <result column="read_count" property="readCount"/>
    <result column="like_count" property="likeCount"/>
    <result column="collect_count" property="collectCount"/>
    <result column="comment_count" property="commentCount"/>
    <result column="read_time" property="readTime"/>
    <result column="is_vip" property="isVip"/>
    <result column="is_collected" property="isCollected"/>
    <result column="is_liked" property="isLiked"/>
    <result column="in_history" property="inHistory"/>
    <result column="featured" property="featured"/>
    <result column="status" property="status"/>
    <result column="sort_order" property="sortOrder"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>

  <resultMap id="WorldNewsIssueResultMap" type="com.interview.auth.domain.entity.WorldNewsIssue">
    <id column="id" property="id"/>
    <result column="issue_key" property="issueKey"/>
    <result column="title" property="title"/>
    <result column="issue_label" property="issueLabel"/>
    <result column="category" property="category"/>
    <result column="today_reads" property="todayReads"/>
    <result column="recommendation" property="recommendation"/>
    <result column="description" property="description"/>
    <result column="cover_accent" property="coverAccent"/>
    <result column="cover_kicker" property="coverKicker"/>
    <result column="cover_headline" property="coverHeadline"/>
    <result column="cover_summary" property="coverSummary"/>
    <result column="cover_footer" property="coverFooter"/>
    <result column="content_html" property="contentHtml"/>
    <result column="published_at" property="publishedAt"/>
    <result column="status" property="status"/>
    <result column="sort_order" property="sortOrder"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>

  <resultMap id="AiHotspotResultMap" type="com.interview.auth.domain.entity.AiHotspot">
    <id column="id" property="id"/>
    <result column="hotspot_key" property="hotspotKey"/>
    <result column="track" property="track"/>
    <result column="hotspot_type" property="hotspotType"/>
    <result column="title" property="title"/>
    <result column="summary" property="summary"/>
    <result column="author_name" property="authorName"/>
    <result column="published_at" property="publishedAt"/>
    <result column="cover_url" property="coverUrl"/>
    <result column="tag_list" property="tagList"/>
    <result column="view_count" property="viewCount"/>
    <result column="comment_count" property="commentCount"/>
    <result column="like_count" property="likeCount"/>
    <result column="heat" property="heat"/>
    <result column="is_recommended" property="isRecommended"/>
    <result column="is_today" property="isToday"/>
    <result column="status" property="status"/>
    <result column="sort_order" property="sortOrder"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>

  <sql id="WorldNewsBaseColumns">
    id,
    issue_key,
    title,
    issue_label,
    category,
    today_reads,
    recommendation,
    description,
    cover_accent,
    cover_kicker,
    cover_headline,
    cover_summary,
    cover_footer,
    content_html,
    published_at,
    status,
    sort_order,
    created_at,
    updated_at
  </sql>

  <sql id="WorldNewsKeywordCondition">
    (
      title LIKE #{keyword}
      OR issue_label LIKE #{keyword}
      OR category LIKE #{keyword}
      OR description LIKE #{keyword}
      OR cover_kicker LIKE #{keyword}
      OR cover_headline LIKE #{keyword}
      OR cover_summary LIKE #{keyword}
      OR cover_footer LIKE #{keyword}
      OR content_html LIKE #{keyword}
    )
  </sql>

  <select id="findPublishedTechArticles" resultMap="TechArticleResultMap">
    SELECT
      id,
      article_key,
      category,
      title,
      summary,
      essence,
      highlight_list,
      author_name,
      author_role,
      author_initials,
      author_accent,
      cover_url,
      content_html,
      published_at,
      read_count,
      like_count,
      collect_count,
      comment_count,
      read_time,
      is_vip,
      is_collected,
      is_liked,
      in_history,
      featured,
      status,
      sort_order,
      created_at,
      updated_at
    FROM tech_article
    WHERE status = 1
    ORDER BY featured DESC, published_at DESC, sort_order ASC
  </select>

  <select id="findPublishedWorldNewsIssues" resultMap="WorldNewsIssueResultMap">
    SELECT
      <include refid="WorldNewsBaseColumns"/>
    FROM world_news_issue
    WHERE status = 1
    ORDER BY published_at DESC, sort_order ASC
  </select>

  <select id="searchWorldNewsIssues" resultMap="WorldNewsIssueResultMap">
    SELECT
      <include refid="WorldNewsBaseColumns"/>
    FROM world_news_issue
    WHERE status = 1
      AND <include refid="WorldNewsKeywordCondition"/>
    ORDER BY published_at DESC, today_reads DESC, recommendation DESC, sort_order ASC
    LIMIT #{size} OFFSET #{offset}
  </select>

  <select id="countWorldNewsIssuesByKeyword" resultType="long">
    SELECT COUNT(*)
    FROM world_news_issue
    WHERE status = 1
      AND <include refid="WorldNewsKeywordCondition"/>
  </select>

  <select id="suggestWorldNewsKeywords" resultType="java.lang.String">
    SELECT suggestion
    FROM (
      SELECT title AS suggestion, published_at
      FROM world_news_issue
      WHERE status = 1 AND title LIKE #{keyword}
      UNION
      SELECT cover_headline AS suggestion, published_at
      FROM world_news_issue
      WHERE status = 1 AND cover_headline LIKE #{keyword}
      UNION
      SELECT cover_kicker AS suggestion, published_at
      FROM world_news_issue
      WHERE status = 1 AND cover_kicker LIKE #{keyword}
    ) suggestion_pool
    WHERE suggestion IS NOT NULL AND suggestion != ''
    ORDER BY published_at DESC
    LIMIT #{size}
  </select>

  <select id="findWorldNewsRanking" resultMap="WorldNewsIssueResultMap">
    SELECT
      <include refid="WorldNewsBaseColumns"/>
    FROM world_news_issue
    WHERE status = 1
    ORDER BY
      <choose>
        <when test="type == 'newbook'">
          published_at DESC, recommendation DESC, today_reads DESC, sort_order ASC
        </when>
        <when test="type == 'overall'">
          recommendation DESC, today_reads DESC, published_at DESC, sort_order ASC
        </when>
        <when test="type == 'masterpiece'">
          CASE WHEN recommendation &gt;= 80 THEN 0 ELSE 1 END ASC,
          recommendation DESC,
          today_reads DESC,
          published_at DESC,
          sort_order ASC
        </when>
        <otherwise>
          today_reads DESC, recommendation DESC, published_at DESC, sort_order ASC
        </otherwise>
      </choose>
    LIMIT #{size} OFFSET #{offset}
  </select>

  <select id="findPopularWorldNewsIssues" resultMap="WorldNewsIssueResultMap">
    SELECT
      <include refid="WorldNewsBaseColumns"/>
    FROM world_news_issue
    WHERE status = 1
    ORDER BY today_reads DESC, recommendation DESC, published_at DESC, sort_order ASC
    LIMIT #{size}
  </select>

  <select id="findWorldNewsIssueByKey" resultMap="WorldNewsIssueResultMap">
    SELECT
      <include refid="WorldNewsBaseColumns"/>
    FROM world_news_issue
    WHERE issue_key = #{issueKey}
      AND status = 1
    LIMIT 1
  </select>

  <select id="findPublishedAiHotspots" resultMap="AiHotspotResultMap">
    SELECT
      id,
      hotspot_key,
      track,
      hotspot_type,
      title,
      summary,
      author_name,
      published_at,
      cover_url,
      tag_list,
      view_count,
      comment_count,
      like_count,
      heat,
      is_recommended,
      is_today,
      status,
      sort_order,
      created_at,
      updated_at
    FROM ai_hotspot
    WHERE status = 1
    ORDER BY sort_order ASC, published_at DESC
  </select>

  <update id="incrementReadCount">
    UPDATE tech_article
    SET read_count = read_count + 1
    WHERE article_key = #{articleKey} AND status = 1
  </update>

  <select id="findReadCount" resultType="java.lang.Integer">
    SELECT read_count FROM tech_article WHERE article_key = #{articleKey} AND status = 1
  </select>

  <insert id="insertArticleComment">
    INSERT INTO article_comment (article_key, nickname, content, avatar_text, avatar_accent, parent_id)
    VALUES (#{articleKey}, #{nickname}, #{content}, #{avatarText}, #{avatarAccent}, #{parentId})
  </insert>

  <update id="incrementCommentCount">
    UPDATE tech_article
    SET comment_count = comment_count + 1
    WHERE article_key = #{articleKey} AND status = 1
  </update>

  <select id="findArticleComments" resultType="java.util.Map">
    SELECT
      id,
      article_key AS articleKey,
      nickname,
      content,
      avatar_text AS avatarText,
      avatar_accent AS avatarAccent,
      parent_id AS parentId,
      like_count AS likeCount,
      created_at AS createdAt
    FROM article_comment
    WHERE article_key = #{articleKey} AND status = 1
    ORDER BY created_at ASC
  </select>

  <update id="softDeleteComment">
    UPDATE article_comment
    SET status = -1
    WHERE id = #{commentId} AND status = 1
  </update>

  <update id="decrementCommentCount">
    UPDATE tech_article
    SET comment_count = GREATEST(comment_count - 1, 0)
    WHERE article_key = #{articleKey} AND status = 1
  </update>

  <select id="findArticleKeyByCommentId" resultType="java.lang.String">
    SELECT article_key FROM article_comment WHERE id = #{commentId} AND status = 1
  </select>

  <update id="markArticleInHistory">
    UPDATE tech_article
    SET in_history = 1
    WHERE article_key = #{articleKey} AND status = 1 AND in_history = 0
  </update>
</mapper>
