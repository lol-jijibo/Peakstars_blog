<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.interview.auth.infrastructure.mapper.ContentMapper">

  <resultMap id="TechArticleResultMap" type="com.interview.auth.domain.entity.TechArticle">
    <id column="id" property="id"/>
    <result column="article_key" property="articleKey"/>
    <result column="category" property="category"/>
    <result column="category_label" property="categoryLabel"/>
    <result column="title" property="title"/>
    <result column="summary" property="summary"/>
    <result column="essence" property="essence"/>
    <result column="highlight_list" property="highlightList"/>
    <result column="author_name" property="authorName"/>
    <result column="author_role" property="authorRole"/>
    <result column="author_initials" property="authorInitials"/>
    <result column="author_accent" property="authorAccent"/>
    <result column="cover_url" property="coverUrl"/>
    <result column="content_html" property="contentHtml"/>
    <result column="published_at" property="publishedAt"/>
    <result column="read_count" property="readCount"/>
    <result column="like_count" property="likeCount"/>
    <result column="collect_count" property="collectCount"/>
    <result column="comment_count" property="commentCount"/>
    <result column="read_time" property="readTime"/>
    <result column="is_vip" property="isVip"/>
    <result column="is_collected" property="isCollected"/>
    <result column="is_liked" property="isLiked"/>
    <result column="in_history" property="inHistory"/>
    <result column="featured" property="featured"/>
    <result column="status" property="status"/>
    <result column="sort_order" property="sortOrder"/>
    <result column="last_read_at" property="lastReadAt"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>

  <resultMap id="WorldNewsIssueResultMap" type="com.interview.auth.domain.entity.WorldNewsIssue">
    <id column="id" property="id"/>
    <result column="issue_key" property="issueKey"/>
    <result column="title" property="title"/>
    <result column="issue_label" property="issueLabel"/>
    <result column="category" property="category"/>
    <result column="today_reads" property="todayReads"/>
    <result column="recommendation" property="recommendation"/>
    <result column="description" property="description"/>
    <result column="cover_accent" property="coverAccent"/>
    <result column="cover_kicker" property="coverKicker"/>
    <result column="cover_headline" property="coverHeadline"/>
    <result column="cover_summary" property="coverSummary"/>
    <result column="cover_footer" property="coverFooter"/>
    <result column="content_html" property="contentHtml"/>
    <result column="published_at" property="publishedAt"/>
    <result column="status" property="status"/>
    <result column="sort_order" property="sortOrder"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>

  <sql id="WorldNewsBaseColumns">
    id,
    issue_key,
    title,
    issue_label,
    category,
    today_reads,
    recommendation,
    description,
    cover_accent,
    cover_kicker,
    cover_headline,
    cover_summary,
    cover_footer,
    content_html,
    published_at,
    status,
    sort_order,
    created_at,
    updated_at
  </sql>

  <sql id="WorldNewsKeywordCondition">
    (
      title LIKE #{keyword}
      OR issue_label LIKE #{keyword}
      OR category LIKE #{keyword}
      OR description LIKE #{keyword}
      OR cover_kicker LIKE #{keyword}
      OR cover_headline LIKE #{keyword}
      OR cover_summary LIKE #{keyword}
      OR cover_footer LIKE #{keyword}
      OR content_html LIKE #{keyword}
    )
  </sql>

  <sql id="TechArticleCategoryCondition">
    <if test="category != null and category != '' and category != 'all'">
      <choose>
        <when test="category == 'vip'">
          AND is_vip = 1
        </when>
        <when test="category == 'history'">
          AND in_history = 1
        </when>
        <when test="category == 'collect'">
          AND is_collected = 1
        </when>
        <when test="category == 'like'">
          AND is_liked = 1
        </when>
        <otherwise>
          AND category = #{category}
        </otherwise>
      </choose>
    </if>
  </sql>

  <sql id="TechArticleUserCategoryCondition">
    <if test="category != null and category != '' and category != 'all'">
      <choose>
        <when test="category == 'vip'">
          AND article.is_vip = 1
        </when>
        <when test="category == 'history'">
          AND history.last_read_at IS NOT NULL
        </when>
        <when test="category == 'collect'">
          AND article.is_collected = 1
        </when>
        <when test="category == 'like'">
          AND article_like.id IS NOT NULL
        </when>
        <otherwise>
          AND article.category = #{category}
        </otherwise>
      </choose>
    </if>
  </sql>

  <select id="findPublishedTechArticles" resultMap="TechArticleResultMap">
    SELECT
      id,
      article_key,
      category,
      category_label,
      title,
      summary,
      essence,
      highlight_list,
      author_name,
      author_role,
      author_initials,
      author_accent,
      cover_url,
      content_html,
      published_at,
      read_count,
      like_count,
      collect_count,
      comment_count,
      read_time,
      is_vip,
      is_collected,
      is_liked,
      in_history,
      featured,
      status,
      sort_order,
      NULL AS last_read_at,
      created_at,
      updated_at
    FROM tech_article
    WHERE status = 1
    <include refid="TechArticleCategoryCondition"/>
    ORDER BY featured DESC, published_at DESC, sort_order ASC
    LIMIT #{size} OFFSET #{offset}
  </select>

  <!-- StarRead 轻量查询：排除 content_html(MEDIUMTEXT)，避免首页聚合时全量加载大字段 -->
  <select id="findPublishedTechArticlesLight" resultMap="TechArticleResultMap">
    SELECT
      id,
      article_key,
      category,
      category_label,
      title,
      summary,
      essence,
      highlight_list,
      author_name,
      author_role,
      author_initials,
      author_accent,
      cover_url,
      NULL AS content_html,
      published_at,
      read_count,
      like_count,
      collect_count,
      comment_count,
      read_time,
      is_vip,
      is_collected,
      is_liked,
      in_history,
      featured,
      status,
      sort_order,
      NULL AS last_read_at,
      created_at,
      updated_at
    FROM tech_article
    WHERE status = 1
    <include refid="TechArticleCategoryCondition"/>
    ORDER BY featured DESC, published_at DESC, sort_order ASC
    LIMIT #{size} OFFSET #{offset}
  </select>

  <select id="findPublishedTechArticlesByUser" resultMap="TechArticleResultMap">
    SELECT
      article.id,
      article.article_key,
      article.category,
      article.category_label,
      article.title,
      article.summary,
      article.essence,
      article.highlight_list,
      article.author_name,
      article.author_role,
      article.author_initials,
      article.author_accent,
      article.cover_url,
      article.content_html,
      article.published_at,
      article.read_count,
      article.like_count,
      article.collect_count,
      article.comment_count,
      article.read_time,
      article.is_vip,
      article.is_collected,
      CASE WHEN article_like.id IS NULL THEN 0 ELSE 1 END AS is_liked,
      CASE WHEN history.last_read_at IS NULL THEN 0 ELSE 1 END AS in_history,
      article.featured,
      article.status,
      article.sort_order,
      history.last_read_at,
      article.created_at,
      article.updated_at
    FROM tech_article article
    LEFT JOIN tech_article_read_history history
      ON history.article_key = article.article_key
      AND history.user_id = #{userId}
    LEFT JOIN tech_article_like article_like
      ON article_like.article_key = article.article_key
      AND article_like.user_id = #{userId}
    WHERE article.status = 1
    <include refid="TechArticleUserCategoryCondition"/>
    ORDER BY article.featured DESC, article.published_at DESC, article.sort_order ASC
    LIMIT #{size} OFFSET #{offset}
  </select>

  <select id="countPublishedTechArticles" resultType="long">
    SELECT COUNT(*)
    FROM tech_article
    WHERE status = 1
    <include refid="TechArticleCategoryCondition"/>
  </select>

  <select id="countPublishedTechArticlesByUser" resultType="long">
    SELECT COUNT(*)
    FROM tech_article article
    LEFT JOIN tech_article_read_history history
      ON history.article_key = article.article_key
      AND history.user_id = #{userId}
    LEFT JOIN tech_article_like article_like
      ON article_like.article_key = article.article_key
      AND article_like.user_id = #{userId}
    WHERE article.status = 1
    <include refid="TechArticleUserCategoryCondition"/>
  </select>

  <select id="countTechArticlesByCategory" resultType="java.util.Map">
    SELECT
      'all' AS category,
      COUNT(*) AS cnt
    FROM tech_article
    WHERE status = 1

    UNION ALL

    SELECT
      'frontend' AS category,
      COUNT(*) AS cnt
    FROM tech_article
    WHERE status = 1 AND category = 'frontend'

    UNION ALL

    SELECT
      'backend' AS category,
      COUNT(*) AS cnt
    FROM tech_article
    WHERE status = 1 AND category = 'backend'

    UNION ALL

    SELECT
      'project' AS category,
      COUNT(*) AS cnt
    FROM tech_article
    WHERE status = 1 AND category = 'project'

    UNION ALL

    SELECT
      'vip' AS category,
      COUNT(*) AS cnt
    FROM tech_article
    WHERE status = 1 AND is_vip = 1

    <if test="userId != null">
      UNION ALL

      SELECT
        'history' AS category,
        COUNT(*) AS cnt
      FROM tech_article article
      INNER JOIN tech_article_read_history history
        ON history.article_key = article.article_key
        AND history.user_id = #{userId}
      WHERE article.status = 1

      UNION ALL

      SELECT
        'collect' AS category,
        COUNT(*) AS cnt
      FROM tech_article
      WHERE status = 1 AND is_collected = 1

      UNION ALL

      SELECT
        'like' AS category,
        COUNT(*) AS cnt
      FROM tech_article article
      INNER JOIN tech_article_like article_like
        ON article_like.article_key = article.article_key
        AND article_like.user_id = #{userId}
      WHERE article.status = 1
    </if>
  </select>

  <select id="findPublishedWorldNewsIssues" resultMap="WorldNewsIssueResultMap">
    SELECT
      <include refid="WorldNewsBaseColumns"/>
    FROM world_news_issue
    WHERE status = 1
    ORDER BY published_at DESC, sort_order ASC
  </select>

  <select id="searchWorldNewsIssues" resultMap="WorldNewsIssueResultMap">
    SELECT
      <include refid="WorldNewsBaseColumns"/>
    FROM world_news_issue
    WHERE status = 1
      AND <include refid="WorldNewsKeywordCondition"/>
    ORDER BY published_at DESC, today_reads DESC, recommendation DESC, sort_order ASC
    LIMIT #{size} OFFSET #{offset}
  </select>

  <select id="countWorldNewsIssuesByKeyword" resultType="long">
    SELECT COUNT(*)
    FROM world_news_issue
    WHERE status = 1
      AND <include refid="WorldNewsKeywordCondition"/>
  </select>

  <select id="suggestWorldNewsKeywords" resultType="java.lang.String">
    SELECT suggestion
    FROM (
      SELECT title AS suggestion, published_at
      FROM world_news_issue
      WHERE status = 1 AND title LIKE #{keyword}
      UNION
      SELECT cover_headline AS suggestion, published_at
      FROM world_news_issue
      WHERE status = 1 AND cover_headline LIKE #{keyword}
      UNION
      SELECT cover_kicker AS suggestion, published_at
      FROM world_news_issue
      WHERE status = 1 AND cover_kicker LIKE #{keyword}
    ) suggestion_pool
    WHERE suggestion IS NOT NULL AND suggestion != ''
    ORDER BY published_at DESC
    LIMIT #{size}
  </select>

  <select id="findWorldNewsRanking" resultMap="WorldNewsIssueResultMap">
    SELECT
      <include refid="WorldNewsBaseColumns"/>
    FROM world_news_issue
    WHERE status = 1
    ORDER BY
      <choose>
        <when test="type == 'newbook'">
          published_at DESC, recommendation DESC, today_reads DESC, sort_order ASC
        </when>
        <when test="type == 'overall'">
          recommendation DESC, today_reads DESC, published_at DESC, sort_order ASC
        </when>
        <when test="type == 'masterpiece'">
          CASE WHEN recommendation &gt;= 80 THEN 0 ELSE 1 END ASC,
          recommendation DESC,
          today_reads DESC,
          published_at DESC,
          sort_order ASC
        </when>
        <otherwise>
          today_reads DESC, recommendation DESC, published_at DESC, sort_order ASC
        </otherwise>
      </choose>
    LIMIT #{size} OFFSET #{offset}
  </select>

  <select id="findPopularWorldNewsIssues" resultMap="WorldNewsIssueResultMap">
    SELECT
      <include refid="WorldNewsBaseColumns"/>
    FROM world_news_issue
    WHERE status = 1
    ORDER BY today_reads DESC, recommendation DESC, published_at DESC, sort_order ASC
    LIMIT #{size}
  </select>

  <select id="findWorldNewsIssueByKey" resultMap="WorldNewsIssueResultMap">
    SELECT
      <include refid="WorldNewsBaseColumns"/>
    FROM world_news_issue
    WHERE issue_key = #{issueKey}
      AND status = 1
    LIMIT 1
  </select>


  <update id="incrementReadCount">
    UPDATE tech_article
    SET read_count = read_count + 1
    WHERE article_key = #{articleKey} AND status = 1
  </update>

  <select id="findReadCount" resultType="java.lang.Integer">
    SELECT read_count FROM tech_article WHERE article_key = #{articleKey} AND status = 1
  </select>

  <select id="countArticleLikeByUser" resultType="int">
    SELECT COUNT(*)
    FROM tech_article_like
    WHERE article_key = #{articleKey}
      AND user_id = #{userId}
  </select>

  <insert id="insertArticleLike">
    INSERT IGNORE INTO tech_article_like (user_id, article_key)
    VALUES (#{userId}, #{articleKey})
  </insert>

  <delete id="deleteArticleLike">
    DELETE FROM tech_article_like
    WHERE article_key = #{articleKey}
      AND user_id = #{userId}
  </delete>

  <update id="incrementLikeCount">
    UPDATE tech_article
    SET like_count = like_count + 1
    WHERE article_key = #{articleKey} AND status = 1
  </update>

  <update id="decrementLikeCount">
    UPDATE tech_article
    SET like_count = GREATEST(like_count - 1, 0)
    WHERE article_key = #{articleKey} AND status = 1
  </update>

  <select id="findLikeCount" resultType="java.lang.Integer">
    SELECT like_count FROM tech_article WHERE article_key = #{articleKey} AND status = 1
  </select>

  <insert id="upsertArticleReadHistory">
    INSERT INTO tech_article_read_history (user_id, article_key, read_count, last_read_at)
    VALUES (#{userId}, #{articleKey}, 1, CURRENT_TIMESTAMP)
    ON DUPLICATE KEY UPDATE
      read_count = read_count + 1,
      last_read_at = CURRENT_TIMESTAMP
  </insert>

  <insert id="insertArticleComment">
    INSERT INTO article_comment (article_key, nickname, content, avatar_text, avatar_accent, parent_id)
    VALUES (#{articleKey}, #{nickname}, #{content}, #{avatarText}, #{avatarAccent}, #{parentId})
  </insert>

  <update id="incrementCommentCount">
    UPDATE tech_article
    SET comment_count = comment_count + 1
    WHERE article_key = #{articleKey} AND status = 1
  </update>

  <select id="findArticleComments" resultType="java.util.Map">
    SELECT
      id,
      article_key AS articleKey,
      nickname,
      content,
      avatar_text AS avatarText,
      avatar_accent AS avatarAccent,
      parent_id AS parentId,
      like_count AS likeCount,
      created_at AS createdAt
    FROM article_comment
    WHERE article_key = #{articleKey} AND status = 1
    ORDER BY created_at ASC
  </select>

  <update id="softDeleteComment">
    UPDATE article_comment
    SET status = -1
    WHERE id = #{commentId} AND status = 1
  </update>

  <update id="decrementCommentCount">
    UPDATE tech_article
    SET comment_count = GREATEST(comment_count - 1, 0)
    WHERE article_key = #{articleKey} AND status = 1
  </update>

  <select id="findArticleKeyByCommentId" resultType="java.lang.String">
    SELECT article_key FROM article_comment WHERE id = #{commentId} AND status = 1
  </select>

  <update id="markArticleInHistory">
    UPDATE tech_article
    SET in_history = 1
    WHERE article_key = #{articleKey} AND status = 1 AND in_history = 0
  </update>
</mapper>
