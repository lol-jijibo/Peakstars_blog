# 代码思路

> 模板文档，替换为实际代码设计

## 技术选型

| 层级 | 技术 | 原因 |
|------|------|------|
| 前端框架 | Vue 3 + Vite | 项目统一技术栈 |
| 状态管理 | Pinia |  | 
| 后端框架 | Spring Boot |  | 
| 数据库 | MySQL |  | 
| 对象存储 | MinIO | 文件/图片存储 | 

## 前端结构

```
src/
├── api/              # API 请求封装
│   └── xxx.js
├── components/       # 公共组件
│   └── XxxSection.vue
├── stores/           # Pinia 状态管理
│   └── xxx.js
├── styles/
│   └── views/
│       └── XxxDetail.css
└── views/            # 页面组件
    └── XxxDetail.vue
```

## 后端结构

```
server-java/src/main/java/com/interview/auth/
├── controller/
│   └── XxxController.java      # HTTP 入口，参数校验，统一响应包装
├── service/
│   ├── XxxService.java          # 接口定义
│   └── impl/
│       └── XxxServiceImpl.java  # 核心业务实现
├── domain/entity/
│   └── Xxx.java                 # 数据库实体
└── infrastructure/mapper/
    └── XxxMapper.java           # MyBatis Mapper
```

## 关键实现

### 前端核心逻辑

```js
// 示例：数据请求与响应处理
const loadData = async () => {
  const res = await api.getList()
  // 数据转换与状态更新
}
```

### 后端核心逻辑

```java
// 示例：Service 层业务编排
public List<XxxResponse> listXxx() {
    // 1. 查询数据
    // 2. 字段转换
    // 3. 返回响应
}
```

## 踩坑记录

1. 问题描述 → 原因分析 → 解决方案
2. 问题描述 → 原因分析 → 解决方案
